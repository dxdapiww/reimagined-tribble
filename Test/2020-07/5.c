#include<stdio.h>
int main(void)
{
    int i,n;
    char id;
    scanf("%d",&n);
    while(id!='\n')
    {
        scanf("%c",&id);
        switch(id){
            case 'X':
            
        }
    }
}